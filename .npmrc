# Disable lifecycle scripts from dependencies (security)
ignore-scripts=true

# Allow our own prepare script for husky
# Run manually after install: npm run prepare

# Strict engine checking
engine-strict=true

# Save exact versions
save-exact=true

# Use lockfile
package-lock=true
